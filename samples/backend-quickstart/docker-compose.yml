# One command: `docker compose up`.
# Configuration comes from .env (copy .env.example to .env and fill it in).
services:
  backend:
    build: .
    ports:
      - "${HOST_PORT:-8080}:8080"
    env_file: .env
